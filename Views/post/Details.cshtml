@model StockGTO.Models.Post
@{
    ViewData["Title"] = @Model.Title;
}

<h2>@Model.Title</h2>
<p class="text-muted">@Model.CreatedAt.ToString("yyyy-MM-dd HH:mm")</p>
<div>
    @Html.Raw(Model.Content.Replace("\n", "<br>"))
</div>
